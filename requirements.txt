# These are minimal requirements to run the code, without pre-commit checks
# etc.

# Generate plots.
matplotlib
# Perform scientific computations.
numpy
# Allow for auto generation of type-hints during runtime.
pyannotate
# Run python tests.
pytest-cov
# Ensure the python function arguments are verified at runtime.
typeguard
